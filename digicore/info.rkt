#lang info

(define collection "digicore")

(define version "Baby")

(define build-deps '{"base" "at-exp-lib" "scribble-lib"})

(define compile-omit-paths 'all)
(define test-omit-paths 'all)

(define pkg-desc "Digicores are shared libraries and frameworks, so leave it alone, since the required source code will be compiled automatically.")
